<%= form_tag question_answers_path(@question) do %>
  <% @answers.each do |answer| %>
        <%= fields_for "answers[][]", answer do |a| %>
          <div class="field">
            <%= a.label :label %>
            <%= a.text_area :label %><br>
            <%= a.label :solution %>
            <%= a.text_area :solution %><br>
            <%= a.label :hint %>
            <%= a.text_area :hint %><br><br><br>
            <%= a.hidden_field :redirect, value: @referer %>
          </div>
        <% end %>
  <% end %>
  <div class="actions">
    <%= submit_tag %>
  </div>
<% end %>
