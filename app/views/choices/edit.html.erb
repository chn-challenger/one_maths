<%= form_for @choice do |f| %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.hidden_field :redirect, value: @referer %>
  <%= f.select :correct, options_for_select([['Mark as the right choice', true], ['Mark as a wrong choice', false]], false) %>
  <%= f.submit %>
<% end %>
